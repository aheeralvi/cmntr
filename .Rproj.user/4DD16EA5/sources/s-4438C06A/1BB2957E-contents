library(shiny)
library(miniUI)

headerCommentDialog <- function() {
  ui <- miniPage(

  ) # End UI
  server <- function(input, output, session) {

  } # End Server
  viewer <- dialogViewer("asdf")
  runGadget(ui, server, viewer = viewer)
} # End Gadget
